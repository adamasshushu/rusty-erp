[package]
name = "frappe-sqlite"
version = "0.2.0"
edition = "2021"

[dependencies]
frappe-core = { path = "../frappe-core" }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
sqlx = { workspace = true }
